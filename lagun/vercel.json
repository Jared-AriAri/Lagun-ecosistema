{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "ng build",
    "outputDirectory": "dist/lagun/browser",
    "framework": "angular",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}